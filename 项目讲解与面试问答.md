# 行旅：项目讲解与面试问答

## 30 秒介绍
行旅把旅行需求录入、AI 规划、人工修改和导出连成一条完整流程。Dify 负责模型与知识检索编排，FastAPI 管理接口、流式输出、账号和数据，浏览器工作台负责编辑与展示。没有模型配置时也能手动规划，不把模板冒充模型生成。

## 先读懂一条链路
浏览器 form → POST /api/travel/stream → TravelRequest 校验 → Dify 请求 → SSE 逐段返回 → 完整结果分析 → SQLite 保存 → 前端显示。修改日程走 PATCH /api/history/{id}/structured，保存 edited_plan，导出优先使用这份结果。

建议先在手动模式完成一次往返，再从 api/main.py 找 create_stream，阅读 history.py 的 save/get、editor.py 的 edited_text，最后读 streaming.py 的异步上游处理。断点观察 inputs、owner、result，不要输出 Key。

## 面试追问
**为什么还需要后端？** Dify 不等于用户产品；账号、历史权限、接口校验、编辑一致性、流式转发与故障处理仍由应用承担。

**RAG 是什么？** 从知识库检索相关材料并注入模型上下文。检索结果是依据，不保证生成完全正确，来源需显示并核实；DSL 导入其他环境后须重新绑定模型和知识库。

**SSE 与普通请求的区别？** 普通请求等全部完成返回，SSE 持续发送事件；客户端解析增量、状态和完成消息。上游中断时不把部分文本保存成成功记录。

**编辑为什么要单独保存？** 原始模型文本用于追溯；edited_plan 作为最新导出依据，防止页面看见修改而下载旧内容。只有服务器保存成功才更新当前状态。

**怎么保护历史？** Cookie 绑定游客或登录会话，后端按 owner 查询，每个读取/修改请求都验证归属；注册/登录时处理游客历史，退出清除页面私有数据。

**外部工具错误怎么处理？** 天气、路线有独立状态与提示；模型请求有超时与异常消息；统计记录失败不破坏已经完成的方案。没有模型时启用手动模式，并明确标注来源。

**有什么工程限制？** SQLite 与内存限流适合单实例；路线为驾车估算；价格、预约与实时营业时间仍需用户核实；没有邮件找回密码和多租户生产运维。

**怎么证明跑通？** 本次 67 项 pytest、真实浏览器完整流程，以及一次真实 Dify 两天生成。区分模拟响应测试和真实服务验收，不把单次成功说成大规模稳定性。

## 准备演示
用虚构需求演示生成、修改一个地点、保存、下载验证、搜索历史与退出账号。面试只能说清实际理解和改动的部分；代码阅读与调试练习优先于背诵术语。维护者与工具说明见 docs/CREDITS.md。
