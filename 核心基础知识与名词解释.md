# 行旅核心知识

| 名词 | 项目中的含义 |
| --- | --- |
| Python / async def / await | 函数可在等待网络时让出执行机会；不是自动启动新线程 |
| FastAPI | 将 URL 与 Python 函数连接，处理输入与返回 |
| Pydantic | 把 JSON 转成有字段约束的对象，验证日期、人数、长度等 |
| Dify / Chatflow | 可视化编排模型、知识检索、条件检查与输出 |
| LLM / Token / Prompt | 语言模型 / 计量与处理文本的片段 / 给模型的任务与约束 |
| RAG / Embedding | 检索知识后生成 / 把文本转成可比较的向量；不是训练新模型 |
| SSE | 服务端通过同一 HTTP 响应持续发送文本事件 |
| HTTPX | 后端请求外部 API 的客户端，支持流式与异步 |
| JSON / Markdown | 接口的结构化数据 / 行程正文的轻量排版文本 |
| SQLite / SQL | 文件型关系数据库 / 查询修改数据的语言 |
| Cookie / owner | 会话标识 / 后端判断一份历史属于谁的依据 |
| 环境变量 / .env | 将地址与密钥放在本地配置，而非源码中 |
| 限流 / 并发上限 | 限制一段时间内请求数 / 同时处理数，避免资源耗尽 |
| 超时 / 降级 | 等待过久停止 / 服务不足时给出可用的较小功能并提示 |
| 幂等 / 版本 | 重复操作结果可预测 / 修订保留先前方案关联 |
| pytest / Playwright | 测试 Python 逻辑和 API / 用真实浏览器操作页面 |
| Docker / 卷 | 打包运行环境 / 在容器重建时保留数据库 |

## 理解 create_stream
`async def create_stream(inputs: TravelRequest, request: Request, response: Response)` 定义一个异步函数。inputs 是校验后的旅行需求，request 包含本次 HTTP 请求和 Cookie，response 用于设置响应信息。冒号后的内容是类型提示，不是给参数赋值。完整业务需继续读函数体。

## 由浅入深的练习
1. 修改一个页面提示文字，刷新确认变化。
2. 调用 /api/health，观察 JSON 和状态码。
3. 在手动模式创建计划，用日志看 inputs 中的日期和人数。
4. 修改地点并下载，追踪 edited_plan 为何优先于 answer。
5. 注册两个账号，验证彼此历史不能访问。
6. 用测试构造上游超时，说明为什么半成品不入库。
